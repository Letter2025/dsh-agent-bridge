{
  "name": "dsh-agent-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Bridge DSH into your agent workflow through Skills and CLI.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsdown && node scripts/validate_build.mjs",
    "skill:build": "pnpm build",
    "skill:artifacts:check": "node scripts/check_skill_artifacts.mjs",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "skill:check": "pnpm skill:artifacts:check && pnpm skill:build && node --check skill/dsh-agent/scripts/dsh_web.mjs && node --check skill/dsh-agent/scripts/run_dsh.mjs && node --check skill/dsh-agent/scripts/dsh_worktree.mjs && pnpm test && node scripts/validate_skill.mjs skill/dsh-agent && node scripts/validate_skill.mjs skill/dsh-worker",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@9.15.4"
}
