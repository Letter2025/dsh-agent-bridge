interface:
  display_name: "Qoder Agent"
  short_description: "Review isolated Qoder changes before applying"
  default_prompt: "Use $qoder-agent to run a bounded Qoder task in an isolated worktree and review its diff before applying it."
