# Runner Protocol Notes

This document reserves the protocol reference for the Qoder Agent Skill.

The initialization milestone does not define or implement a wire protocol.
Future work will document command construction, structured output, timeout
behavior, errors, permissions, and the handoff back to the calling agent.
