interface:
  display_name: "Qoder Worker"
  short_description: "Review isolated Qoder work before applying"
  default_prompt: "Use $qoder-worker to run bounded Qoder work in an isolated worktree and review its diff before applying it."
